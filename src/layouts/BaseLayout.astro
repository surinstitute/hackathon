---
import type { ImageMetadata } from 'astro';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { getSite } from '../utils/consts';
import { getLangFromUrl } from '../i18n/utils';

interface Props {
	title?: string;
	description?: string;
	image?: ImageMetadata;
}

const site = await getSite();
const {
    title = site.title ?? 'Launchpad',
    description = site.description ?? '',
    image,
} = Astro.props;

// determine current language from the URL
const lang = getLangFromUrl(Astro.url);
---

<!doctype html>
<html lang={lang} data-theme="light">
    <head>
        <BaseHead title={title} description={description} image={image} />
    </head>

    <body>
        <Header />
        <main class="content-shell">
            <slot />
        </main>
        <Footer />
    </body>
</html>

<script is:inline src="https://www.unpkg.com/@heyform-inc/embed@latest/dist/index.umd.js"></script>

<style>
    .content-shell {
        padding-top: calc(var(--grid-cell) * 1);
    }

    @media (max-width: 720px) {
        .content-shell {
            padding-top: calc(var(--grid-cell) * 4);
        }
    }
</style>